# ==============================================================================
# データベース接続設定 (PostgreSQL - 'dev' プロファイル専用)
# ==============================================================================
# 【注意】コメントはMySQLになっていますが、設定内容はPostgreSQLです。
spring.datasource.url=jdbc:postgresql://172.16.1.126:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=coto24i1004
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================================================================
# JPA/Hibernate 設定
# ==============================================================================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# 'update'はEntityクラスに基づいてDBのテーブル構造を自動で更新します
spring.jpa.hibernate.ddl-auto=update
# 実行されるSQLをコンソールに表示
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
